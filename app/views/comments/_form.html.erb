<%= form_for([project, comment]) do |f| %>

   <div class="field">
     <%= f.label :comment %><br/>
     <%= f.text_field  :input %>
   </div>

   <div class="actions">
     <%= f.submit %>
   </div>

 <% end %>
